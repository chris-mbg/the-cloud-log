query locationsByCountyAndMunicipality(
  $county: String!
  $municipality: String!
) {
  locations(
    filters: { county: { eq: $county }, municipality: { eq: $municipality } }
  ) {
    data {
      id
      attributes {
        municipality
        locality
      }
    }
  }
}
