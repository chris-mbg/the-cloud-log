mutation deleteObservation($id: ID!) {
  deleteObservation(id: $id) {
    data {
      id
      attributes {
        observation_time
        owner {
          data {
            id
          }
        }
      }
    }
  }
}
